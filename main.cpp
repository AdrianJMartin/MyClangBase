#include <iostream>

int main() {

  double a = std::cos(0.0);
  double b = std::cos(0.1);
  double c = std::cos(0.2);
  double d = std::cos(0.3);

  
  std::cout << a << ", " << b << ", " << c << ", " << d;
}